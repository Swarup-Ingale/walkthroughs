# URL
https://hackmyvm.eu/challenges/challenge.php?c=067

# Concept
To Perform Reverse Engineering on the Binary and get the c code from raw binary using Ghidra and then analyze it to get the flag

# Method of Solve
- Go to the Challenge Interface and Download the raw binary files
- The Static file is the main file which is completely compiled
- So lets open it in ghidra and Decompile it .. we get :
  ```
    
    undefined8 main(void)
    
    {
      int iVar1;
      size_t sVar2;
      uchar local_278 [64];
      char local_238 [144];
      char local_1a8 [140];
      undefined4 local_11c;
      char local_118 [4];
      char acStack_114 [260];
      char *local_10;
      
      printf("Enter the flag: HMV{...}: ");
      fgets(local_118,0x106,(FILE *)stdin);
      sVar2 = strcspn(local_118,"\n");
      local_118[sVar2] = '\0';
      iVar1 = strncmp(local_118,"HMV{",4);
      if ((iVar1 == 0) && (sVar2 = strlen(local_118), local_118[sVar2 - 1] == '}')) {
        sVar2 = strlen(local_118);
        local_118[sVar2 - 1] = '\0';
        local_10 = acStack_114;
        sVar2 = strlen(local_10);
        calculateHash(local_10,sVar2,local_278,&local_11c);
        toHex((long)local_278,(long)local_238,local_11c);
        builtin_strncpy(local_1a8,
                        "3fe8b365cb64286384d9743612d857d938fadb42c359acb69fb9f2f88b96cdbfdb1cacf8a5292aa 004a5efcc01fbdb27d4a72a8dd9a8b34dfff41f9e72bfb734"
                        ,0x81);
        iVar1 = strcmp(local_238,local_1a8);
        if (iVar1 == 0) {
          puts(&DAT_0079e067);
          return 0;
        }
        puts(&DAT_0079e07d);
        return 0;
      }
      puts(&DAT_0079e092);
      return 0;
  ```
- Now we get the hash = **3fe8b365cb64286384d9743612d857d938fadb42c359acb69fb9f2f88b96cdbfdb1cacf8a5292aa004a5efcc01fbdb27d4a72a8dd9a8b34dfff41f9e72bfb734**
- Now lets use hashcat or john to get the value for the hash
- First lets create a hash.txt file and store the hash into it
- Then to find the hashes john support lets find if it supports SHA-512
  ```
    └─$ john --list=formats | grep -i 512
    414 formats (149 dynamic formats shown as just "dynamic_n" here)
    aix-ssha512, andOTP, ansible, argon2, as400-des, as400-ssha1, asa-md5, 
    sha256crypt, sha512crypt, Citrix_NS10, dahua, dashlane, diskcryptor, Django, 
    PBKDF2-HMAC-SHA256, PBKDF2-HMAC-SHA512, PDF, PEM, pfx, pgpdisk, pgpsda, 
    pwsafe, qnx, RACF, RACF-KDFAES, radius, RAdmin, RAKP, rar, RAR5, Raw-SHA512, 
    Salted-SHA1, SSHA512, sapb, sapg, saph, sappse, securezip, 7z, Signal, SIP, 
    skein-256, skein-512, skey, SL3, Snefru-128, Snefru-256, LastPass, SNMP, 
    tc_sha512, tc_whirlpool, vdi, OpenVMS, vmx, VNC, vtp, wbb3, whirlpool, 
    whirlpool0, whirlpool1, wpapsk, wpapsk-pmk, xmpp-scram, xsha, xsha512, zed, 
    HMAC-SHA256, HMAC-SHA384, HMAC-SHA512, dummy, crypt
  ```
- We have to select **Raw-SHA512**
- Now lets crack hash using john
  ```
    └─$ john --format=Raw-SHA512 --wordlist=/usr/share/wordlists/rockyou.txt hash.txt

    Using default input encoding: UTF-8
    Loaded 1 password hash (Raw-SHA512 [SHA512 128/128 SSE2 2x])
    Warning: poor OpenMP scalability for this hash type, consider --fork=4
    Will run 4 OpenMP threads
    Press 'q' or Ctrl-C to abort, almost any other key for status
    i like your smile (?)     
    1g 0:00:00:00 DONE (2026-02-08 06:04) 1.562g/s 11705Kp/s 11705Kc/s 11705KC/s i'msimplythebest..i hate guns
    Use the "--show" option to display all of the cracked passwords reliably
    Session completed.
  ```
- The Flag is **HMV{ilikeyoursmile}**
