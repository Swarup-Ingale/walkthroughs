# URL
https://hackmyvm.eu/challenges/challenge.php?c=047

# Question Attachment Code
```
  import string

  def magic_encode(flag):
      flag, magic_num_list = divmod(flag, 61)
      return f"{string.digits[1:] + string.ascii_uppercase + string.ascii_lowercase}" [magic_num_list]
```

# Method of Solve
- Go to the challenge website
- Read the description carefully and write a python script to decode the magic_nums list
  ```
    import string

    magic_num_list = [12, 43, 36, 47, 21, 40, 23, 42, 14, 54, 10, 53, 14, 36, 32, 40, 28, 50, 22, 40]
    
    charset = string.digits[1:] + string.ascii_uppercase + string.ascii_lowercase
    
    res = ""
    
    for i in magic_num_list:
        tmp = ord(charset[i])-1
        res += chr(tmp)
    
    print("HMV{" + res + "}")
    
    
    def magic_encode(flag):
        flag, magic_num_list = divmod(flag, 61)
        return f"{string.digits[1:] + string.ascii_uppercase + string.ascii_lowercase}"[magic_num_list]
  ```
- After running the code we get the output as flag
- The flag is **HMV{ChalLeNgEsArEaWeSoMe}**
