# Target User
freya  -pass: EEDyYFDwYsmYawj

# Method of Solve
- Login using the ssh credentials
- Perform ls -la to list the files and cat the mission.txt
- Use the uniq command with -u flag to find the one string in different.txt that is not repeated
  ```
    isabel@venus:~$ uniq -u different.txt
    EEDyYFDwYsmYawj
  ```

# Commands Used
- ls -la
- cat
- uniq
