# Target User
isla  -pass: D3XTob0FUImsoBb

# Method of Solve
- Login using the ssh credentials
- perform ls -la to list the files in directory and cat the mission.txt
- unzip the passw0rd.zip file using the unzip or gunzip command
- Get the password
  ```
    victoria@venus:~$ unzip -p passw0rd.zip 
    D3XTob0FUImsoBb
  ```

# Commands Used
- ls -la
- cat
- unzip
