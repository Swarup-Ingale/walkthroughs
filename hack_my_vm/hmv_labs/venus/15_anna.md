# Target User
natalia  -pass: direct access to shell "/bin/sh" of natalia using su **OR** Pass : **NMuc4DkYKDsmZ5z**

# Method of Solve
- Login using the ssh credentials
- Perform ls -la to list all the files and cat the mission.txt
- Use sudo command to get direct access to the shell of natalia user
  ```
    anna@venus:~$ sudo -u natalia /bin/bash
  ```

# Commands Used
- ls -la
- cat
- sudo
